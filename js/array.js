/* Loop ***************************************************************************************** */

list.forEach(function(item, index, originalList) {});








/* Filter *************************************************************************************** */

filteredList = list.filter(function(item) {
    return item === 'this one';
});